<% @page_title = "ユーザ情報の編集" %>

<h1><%= @page_title %></h1>

<div class="toolbar"><%= link_to "ユーザ一の詳細に戻る", [:admin, @member] %></div>

<%= form_for [:admin, @member] do |form| %>
  <%= render "form", form: form %>
  <div><%= form.submit "更新する" %></div>
<% end %>
